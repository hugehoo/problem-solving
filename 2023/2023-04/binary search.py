class Solution:
    def generate(self, numRows: int) -> list[list[int]]:
        result = [[1]]
        for idx in range(1, numRows + 1):
            for i in idx:
                result[i]
            
            
        
        return


s = Solution()
print(s.generate(5))
